/** C3/C4 Socratic + parent dashboard API (offline_backend). */
export const BACKEND_URL =
  process.env.NEXT_PUBLIC_BACKEND_URL || "http://127.0.0.1:8000";

/** C2 Analyst live panel (screen scan + whitebox). */
export const ANALYST_URL =
  process.env.NEXT_PUBLIC_ANALYST_URL || "http://127.0.0.1:8765";
