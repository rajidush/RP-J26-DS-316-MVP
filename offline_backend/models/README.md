"""Drop optional ONNX weights here. They are gitignored; the pipeline runs without them.

  text_hate.onnx        — Phase 4 fine-tuned MiniLM (optional)
  vision_stage1.onnx    — Phase 3 image encoder (optional)
"""
