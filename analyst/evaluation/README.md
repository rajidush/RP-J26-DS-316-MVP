# Evaluation (Milestone A4)

Put benchmark scripts and reports here:

- `baseline_extract.md` — OCR/ASR/CLIP latency & RSS (A1)
- `report.md` — full matrix vs pretrained baselines (A4)

All numbers: reference CPU machine, CUDA disabled, median of 3 runs.
