# Analyst (C2) — core (always install)
pydantic>=2.0.0
pillow>=10.0.0
numpy>=1.24.0

# Step 1 — pretrained Stage-1 text (optional but recommended for demo)
# transformers>=4.36.0
# torch  → install CPU wheel: pip install torch --index-url https://download.pytorch.org/whl/cpu

# Step 2–3 — OCR / ASR
rapidocr-onnxruntime>=1.3.0
faster-whisper>=1.0.0

# Step 4 — CLIP image branch (same torch+transformers as Step 1)
# ANALYST_CLIP_MODEL=openai/clip-vit-base-patch32
