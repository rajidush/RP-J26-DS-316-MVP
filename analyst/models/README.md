# Drop ONNX / CT2 weights here (gitignored). Registry names from Engineering Plan Appendix B:
#   text_fast.onnx, text_full.onnx, vision / CLIP towers, etc.
# The pipeline runs without these files (lexicon + optional OCR/ASR).
